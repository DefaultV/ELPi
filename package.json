{
  "scripts": {
    "build": "rm -rf dist; mkdir dist; cp -r src/resources/* dist/.; tsc",
    "dev": "rm -rf dist; mkdir dist; cp -r src/resources/* dist/.; tsc -w"
  },
  "dependencies": {
    "@types/websocket": "^1.0.5",
    "@types/ws": "^8.5.3",
    "bun-types": "^0.4.0",
    "express": "^4.18.2",
    "typescript": "^4.7.4",
    "websocket": "^1.0.34",
    "ws": "^8.8.1"
  }
}
